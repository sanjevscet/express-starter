{
  "name": "@san/shared",
  "version": "1.0.0",
  "main": "./dist/index.js",
  "license": "MIT",
  "private": true,
  "scripts": {
    "clean": "rm tsconfig.tsbuildinfo && rm -rf ./dist",
    "build": "yarn clean && npx tsc",
    "dev": "concurrently \"npx tsc --watch\""
  },
  "dependencies": {
    "dotenv": "^16.0.1"
  }
}
